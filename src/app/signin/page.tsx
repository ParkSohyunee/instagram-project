import Loginbutton from "@/components/LoginButton";

export default function SigninPage() {
  return (
    <section className="flex items-center justify-center">
      <Loginbutton size="large" type={true} />
    </section>
  );
}
